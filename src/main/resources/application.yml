server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/datasource_one?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

mybatis:
  mapper-locations: classpath:mappers/**/*.xml
